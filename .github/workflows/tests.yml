on: [push]

jobs:
  combine2pyhf:
    runs-on: ubuntu-latest
    name: Run combine and pyhf tests
    steps:
      - uses: cvmfs-contrib/github-action-cvmfs@v3
      - name: Build
        id: build
        uses: combine2pyhf/action@v20230519-1
      
